/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["images.pixels.com", "images.unsplash.com", "i.pinimg.com", "lh3.googleusercontent.com"],
    },
};

export default nextConfig;
